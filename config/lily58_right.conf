# Right half only: this is where the Azoteq TPS43 trackpad is physically
# wired (reusing the OLED header's I2C pins, since this half no longer
# drives a nice!view display - see lily58_right.overlay for the wiring).
CONFIG_I2C=y
CONFIG_INPUT_TPS43=y
